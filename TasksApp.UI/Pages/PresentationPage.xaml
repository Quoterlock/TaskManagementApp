<Page x:Class="TasksApp.UI.Pages.PresentationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TasksApp.UI.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PresentationPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="28"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="weekViewBtn" Click="weekViewBtn_Click">Week</Button>
            <Button x:Name="monthViewBtn" Click="monthViewBtn_Click">Month</Button>
        </StackPanel>
        <Frame x:Name="viewFrame" Grid.Row="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Page>
